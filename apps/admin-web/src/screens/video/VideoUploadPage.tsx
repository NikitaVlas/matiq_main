'use client';
import { useState } from 'react';
import { adminApi } from '../../shared/api/client';
export default function VideoUploadPage(){const [file,setFile]=useState<File>();const [result,setResult]=useState('');const upload=async(e:React.FormEvent)=>{e.preventDefault();if(!file)return;const body=new FormData();body.append('file',file);const r=await adminApi('/admin/videos/upload',{method:'POST',body});setResult(r.ok?`Video created: ${(await r.json()).id}`:'Upload failed')};return <main style={{maxWidth:720,margin:'40px auto',padding:24}}><h1>Upload local video</h1><p>Upload to local MinIO, then use the returned video id when creating a Lesson.</p><form onSubmit={upload}><input type="file" accept="video/mp4,video/webm" required onChange={e=>setFile(e.target.files?.[0])}/><button type="submit">Upload video</button></form>{result&&<p role="status">{result}</p>}</main>}