{
  "name": "@matiq/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/main.ts",
    "lint": "eslint src test",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run test/outbox-dispatcher.test.ts test/idempotent-consumer.test.ts test/observability-server.test.ts",
    "test:integration": "vitest run test/worker.integration.test.ts"
  },
  "dependencies": {
    "@matiq/backend": "workspace:*",
    "@prisma/client": "6.10.1",
    "bullmq": "5.56.0",
    "dotenv": "16.6.1",
    "ioredis": "5.6.1"
  },
  "devDependencies": {
    "tsx": "4.20.3",
    "vitest": "3.2.4"
  }
}
